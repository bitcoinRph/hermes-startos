# Assets

Package assets and branding notes.

- `../icon.png` is the StartOS package icon, generated from the official upstream Hermes Agent website logo at `upstream-project/website/static/img/logo.png`.
- `acp-registry-icon.svg` keeps the upstream ACP registry glyph for reference, but it is not used as the StartOS package icon.
