{
  "name": "hermes-agent-startos",
  "private": true,
  "version": "0.20.1-0",
  "description": "StartOS service package for Hermes Agent — self-improving AI agent from Nous Research.",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "rm -rf ./javascript && ncc build startos/index.ts -o ./javascript",
    "prettier": "prettier --write .",
    "format": "prettier --check ."
  },
  "dependencies": {
    "@start9labs/start-sdk": "2.0.9"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "@vercel/ncc": "^0.38.4",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}
